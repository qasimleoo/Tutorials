# Why numpy rather than list, dict, tuple of python??

# NumPy provides efficient storage .. than python's built datatypes like lists, dict, tuples
# It also provides better ways of handling data for processing.. 
# It is fast
# It is easy to learn - axis
# NumPy uses relatively less memory to store data - smartly managed memory like pointers in C++

# ─────────────────────────────────────────────
# Pre-requisites
# you can't learn numpy directly .. you need to learn python first
# you need to have a basic knowledge of jupyter notebook

# Numpy
# [1,2,3,4,5,6] -> array/vector 
# [[1,2,3,4], [5,6,7,8]] -> matrix/matrices
# Multi dimensions

# basis of ML are matrices .. 
# C++ - efficient (why not assembly .. 'cause difficult so C++)
# numpy is written in numpy -- C++ is also difficult so devs made an interface so that can be used in python .. but behind the scenes it is C++
# ─────────────────────────────────────────────


# ─────────────────────────────────────────────
# other related libraries pandas/pytorch
# we'll use VS Code with jupyter notebooks

# ~ python3 -m venv venv
# ~ source venv/bin/activate
# ~ pip install numpy
# Make a file named `FILE.ipynb`
# 
# 
# 

# ─────────────────────────────────────────────